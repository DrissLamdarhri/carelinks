# Dependencies
node_modules/
package-lock.json
yarn.lock
# NOTE: pnpm-lock.yaml must be tracked so EAS/CI can install reproducibly.

# Environment variables
.env
.env.local
.env.*.local
.env*.example

# IDE
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
*.iml

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# OS
.DS_Store
Thumbs.db

# Build outputs
dist/
build/
.next/
out/

# Large archives (do not commit)
*.zip

# Native projects (managed/CNG workflow — EAS regenerates these via prebuild)
mobile-app/android/
mobile-app/ios/

# Expo
.expo/
.expo-shared/

# Temp files
.cache/
*.tmp
temp/

# Supabase
# Migrations are the schema's source of truth and MUST be versioned. This used to
# ignore them, so every one had to be force-added and 0028 was silently dropped
# from a commit before anyone noticed.
supabase/.temp/
supabase/.env*

# Backups — MUST NEVER be committed. Contains real patient CIN numbers, phone
# numbers, medical/booking data and pro bank RIBs. See scripts/backup/ and
# docs/BACKUP_AND_DISASTER_RECOVERY.md. Store the produced archive in an
# encrypted, access-controlled location outside this repo.
/backups/
scripts/backup/.env.backup
*.sql.gz
*.dump

# Testing
coverage/
.nyc_output/

# Misc
*.pem
.turbo/

# IDE specific
.vscode/extensions.json
.vscode/settings.json

# Mac
.AppleDouble
.LSOverride
*.swp
